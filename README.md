# Telegram Echo Bot (Telegraf)

This repository contains a minimal Telegram Echo Bot built with Telegraf.

Setup:
- Install dependencies: npm install
- Run: TELEGRAM_BOT_TOKEN=your_token npm start

Environment:
- TELEGRAM_BOT_TOKEN: Telegram bot token provided by BotFather

Notes:
- The bot currently echoes any text it receives.
